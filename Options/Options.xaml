<UserControl x:Class="Marakas.Tabs.Options"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Marakas"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer VerticalAlignment="Top" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" VerticalAlignment="Top" Margin="20" Width="300" HorizontalAlignment="Left">
                <TextBlock Margin="0 0 0 20">Mes périphériques d'entrées</TextBlock>
                <ComboBox x:Name="comboInputDevices" Margin="0 0 0 20">
                </ComboBox>
                <TextBlock Margin="0 0 0 20">Mes périphériques de sorties (Output Virtual Mic)</TextBlock>
                <ComboBox x:Name="comboOutputDevices" Margin="0 0 0 20">
                </ComboBox>
                <TextBlock Margin="0 0 0 20">Mes périphériques de sorties (Output Feedback)</TextBlock>
                <ComboBox x:Name="comboOutputDevicesPersonal" Margin="0 0 0 20">
                </ComboBox>
                <DockPanel Margin="0 0 0 20">
                    <TextBox Name="VolumeTextBox" PreviewTextInput="NumberValidationTextBox" Text="{Binding ElementName=gainValue, Path=Value, UpdateSourceTrigger=PropertyChanged}" DockPanel.Dock="Right" TextAlignment="Right" Width="40"/>
                    <Slider Margin="0 0 20 0" Maximum="100" IsSnapToTickEnabled="True" TickFrequency="1" Value="100" Name="gainValue" ValueChanged="VolumeSlider_ValueChanged"/>
                </DockPanel>
                <CheckBox x:Name="earFeedback" Margin="0 0 0 20" Checked="EaringFeedback_CheckedChanged" Unchecked="EaringFeedback_CheckedChanged">Écouter le retour</CheckBox>
                <TextBlock Margin="0 0 0 20">
        Chemin d'accès pour les sons
                </TextBlock>
                <StackPanel Margin="0 0 0 20" Orientation="Horizontal">
                    <Border BorderThickness="1" BorderBrush="Black">
                        <TextBlock Name="PathSoundsTextBox" TextAlignment="Left" Background="White" Padding="5" TextTrimming="CharacterEllipsis" Width="230"></TextBlock>
                    </Border>
                    <Button Click="PathSoundsButton_OpenDialog" Width="30" Margin="5 0 0 0">. . .</Button>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
